# Server-side only. Never prefix these variables with VITE_.
DOCUMENT_ASSIST_ENABLED=false
OPENAI_API_KEY=
OPENAI_DOCUMENT_MODEL=gpt-5.6

# Optional. OpenFIGI works without a key at a lower public rate limit.
OPENFIGI_API_KEY=
